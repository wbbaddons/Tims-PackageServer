<!doctype html>
<html>
	<head>
		<title>{{title}}</title>
	</head>
	<body>
		<p>{{__ "The following source files are available for download:"}}</p>
		<ul>
			{{#files}}
				<li><a href="{{../host}}/source/{{this}}">{{this}}</a></li>
			{{/files}}
		</ul>
	</body>
</html>
